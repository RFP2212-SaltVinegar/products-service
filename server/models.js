// const client = require('./db/connection.js');

module.exports = {
  // return promises
  getProduct: () => {
    // return await client.query();
  },

  getProductStyles: () => {
    // return await client.query();
  },

  getRelatedProducts: () => {
    // return await client.query();
  }
}